{
  "buildCommand": "cd shop-debt-owner-admin && npm install && npm run build",
  "outputDirectory": "shop-debt-owner-admin/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
